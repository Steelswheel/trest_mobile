import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export const NoPage = () => {
  return (
    <SafeAreaView>
      <Text>Нет страницы</Text>
    </SafeAreaView>
  )
}
